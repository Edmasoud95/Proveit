# TODO

## Pending

- [ ] Make the generate stubs button more UX-friendly by only generating stubs for empty tools, and add a generate stubs button on individual tools
  - Improves tool editing experience in `/poc/:id` editor; avoids overwriting existing tool stubs; enables granular stub generation
- [ ] Fix generate evals and test data UX by highlighting what data will be generated vs what is already generated, especially when asking to add new test cases
  - Clarify in `/poc/:id/evals` UI which eval cases are new vs existing; improve feedback when running generate evals with partial data

## In Progress

## Done
